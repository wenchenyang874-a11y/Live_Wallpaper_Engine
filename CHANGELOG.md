# Changelog

本文件记录 Live Wallpaper Engine 的重要变更。

项目采用 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的结构，并遵循[语义化版本](https://semver.org/lang/zh-CN/)；`0.x` 为预览版本。

## [Unreleased]

### Added

- 增加可勾选任意显示器的“显示位置”下拉菜单，并支持同一壁纸在多个所选屏幕内分别填充或跨屏扩展。
- 增加壁纸缩略图、右键预览、重命名和应用操作。
- 增加主界面与托盘“取消应用当前壁纸”入口。
- 增加本进程 CPU、GPU、内存和显存实时占用栏。

### Changed

- 视频改用 Media Engine 帧服务器和共享 D3D11 交换链呈现，切换媒体时不再销毁并重建壁纸渲染器。
- 多显示器视频每帧只执行一次 Media Foundation 帧转换，再由 D3D11 着色器以全不透明画面分发到所选屏幕；高于 1080p 的视频呈现限制为最高约 30 FPS。
- 视频多屏着色器直接绘制到交换链，移除与管理窗口闪烁无关的全桌面中间纹理和额外 `CopyResource`。
- 类型筛选改为“全部 / 图片 / GIF / 视频”四个常驻分段按钮。
- 移除主界面左下角与标题栏重复的隐藏到托盘和退出按钮。

### Fixed

- 修复 Windows 11 raised-desktop 下把渲染窗口放入系统壁纸 WorkerW、导致图片和视频呈现成功但不可见的问题。
- 修复视频切换时重建渲染器、桌面层在相邻帧之间暴露并产生闪烁的问题。
- 修复腾讯桌面整理兼容互斥量未持续持有、第三方桌面层可能反复切换的问题。
- 修复 owner-draw 原生下拉列表中的中文类型名称显示乱码问题。
- 修复多显示器视频对每个屏幕重复执行 `TransferVideoFrame`，造成 GPU 占用偏高并暴露中间帧的问题。
- 修复资源栏把同一进程的多个 GPU 引擎相加、显示值约为任务管理器进程页两倍的问题。
- 修复取消后立即重新应用视频时，旧 Media Engine 会话的延迟事件污染新会话的问题。
- 修复视频播放事件反复触发管理窗口整窗重绘、父窗口覆盖子控件后再分别重画，导致“我的壁纸”窗口持续闪烁的问题；相同状态不再重绘，资源栏改为局部刷新，并增加子控件裁剪和父窗口双缓冲。

## [0.1.0] - 2026-08-21

### Added

- 建立 C++20、Win32 和 Direct3D 11 原生项目骨架。
- 增加 Windows 11 raised-desktop、经典 WorkerW 和 Progman 回退路径的桌面宿主探测。
- 增加不抢焦点、不进入 Alt+Tab 且不拦截桌面输入的壁纸窗口。
- 增加 D3D11 测试渲染器，以及硬件设备失败后的 WARP 回退。
- 增加 `--test-seconds=N` 受控运行参数。
- 增加 `%LOCALAPPDATA%\LiveWallpaperEngine\logs` 本地运行日志。
- 增加单实例协调；重复启动时唤醒已有状态窗口。
- 增加状态窗口、托盘图标、隐藏到托盘和正常退出入口。
- 增加 JPG/JPEG、PNG、BMP 静态图片选择；通过 WIC 校验实际容器、居中裁剪和缩放，再呈现到桌面覆盖层。
- 增加静态壁纸单帧呈现和事件驱动空闲等待，不修改 Windows 原壁纸，退出后原壁纸自然显露。
- 增加版本化本地 JSON 设置，使用临时文件和原子替换保存所选静态图片，并在下次启动时恢复。
- 增加 `--test-static-image=PATH` 受控测试参数和静态覆盖层回归脚本。
- 增加“我的壁纸”本地库、实时搜索和静态图片/GIF/视频类型筛选。
- 增加 WIC 动态 GIF 按需逐帧合成、边界限制和循环呈现。
- 增加 Media Foundation/Media Engine 视频循环播放，默认静音并支持声音开关。
- 增加会话锁定、睡眠、显示器关闭和全屏应用下的动态播放暂停策略。
- 增加原始壁纸与 `.lwewall` 分享包的导入、拖放导入和导出；分享包校验文件名、长度、类型与 SHA-256。
- 增加深色、自绘、响应式的现代化壁纸管理界面。
- 增加设置格式 v1 到 v2 的兼容迁移，保存图片/GIF/视频类型与声音状态。
- 增加 GIF/视频、声音开关、搜索筛选和壁纸包安全校验的受控回归入口。
- 增加 Visual Studio 2022 x64 Debug/Release 构建配置。
- 增加无需管理员权限的 Inno Setup x64 安装包配置。
- 增加三段式 tag 校验、安装包构建、SHA-256 生成和 GitHub Release 上传工作流。
- 增加由发布版本号生成的 Windows 可执行文件版本资源。
- 采用 Apache License 2.0 开源许可。

### Changed

- Release 和 Debug 均静态链接 MSVC CRT，降低目标机器的运行库依赖。
- 加快并增强 D3D11 蓝色/青色测试脉冲，使渲染状态更容易辨认。
- 日志改为 Win32 句柄追加 UTF-8，中文路径不会使后续日志写入失效。

### Fixed

- 修复目标机器加载旧版 `MSVCP140.dll` 时，标准库文件系统调用可能触发启动崩溃的问题。
- 修复 `CreateWindowExW` 创建阶段尚未返回窗口句柄时，早期窗口消息可能使用空句柄的问题。
- 修复反复双击程序会启动多个独立渲染进程的问题。
- 修复日志写入所选中文路径后被截断并停止记录的问题。

### Known limitations

- 当前可在任意显示器组合上分别填充同一壁纸，但尚不支持不同显示器同时使用不同壁纸或独立缩放模式。
- 覆盖层当前为全不透明；带透明通道的 PNG 尚未按 Windows 原壁纸进行背景合成。
- 媒体探测与解码当前仍在主进程内；后续需继续加强不可信媒体隔离。
- Windows 10 经典桌面、真实锁屏/睡眠/熄屏、Explorer 重启和完整桌面图标交互仍待扩大验证。
